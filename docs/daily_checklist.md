## STEP 1: 프로젝트 기획서 작성 (8/1 완료)

#### - GitHub Repository 생성
#### -  프로젝트 폴더 구조 생성
#### -  requirements.txt 작성
#### -  .gitignore 작성
#### -  Python 가상환경 생성 및 활성화
#### -  pip install -r requirements.txt 실행
#### -  MySQL 설치 및 접속 확인

## Step 2. 데이터베이스 환경 구축 (8/3 완료)

#### - MySQL 사용 결정
#### - fate 데이터베이스 생성
#### - MySQL DB 생성
#### - Python DB 연결

## Step 3. ETL 적재 검증 + 파이썬 연결 (8/23 완료)

#### - MySQL 스키마 및 핵심 테이블
#### - 종목 주가·KOSPI 수집/증분 적재 ETL
#### - ETL 실행 이력 관리
#### - 초기 적재·조회 검증 결과
#### - 환경변수 및 실행 방법
#### - MySQL 기준 개발 일정 완료 현황

## STEP 4: KOSPI·KOSDAQ 전체 종목 초기 적재 (8/24 완료)

#### - KOSPI·KOSDAQ 전체 종목 마스터를 `stocks` 테이블에 동기화
#### - 전체 미적재 종목의 최근 1년 일별 OHLCV를 `stock_prices`에 초기 적재
#### - 이후 증분 적재와 종목명·종목코드 기반 대시보드 구현을 위한 데이터 기반 확보

## STEP 5: 예측 모델·대시보드·수급 데이터 기반 구축 (8/27 완료)

#### - 기술지표 15개를 사용한 다음 거래일 상승 여부 분류 모델 학습
#### - Logistic Regression·XGBoost·Random Forest 성능 비교 및 XGBoost 선택
#### - 최신 종목별 상승 확률 예측 파일 생성
#### - Streamlit 예측 대시보드 구현: 종목 검색, 방향·확률 필터, 확률 순위 차트, CSV 다운로드
#### - 워크포워드 검증 및 예측-실제 성과 추적 스크립트 추가
#### - 업종 프로필·외국인/기관/개인 순매수 데이터용 MySQL 테이블 설계
#### - KRX 웹 기반 수급 적재기는 접근 제한 및 이용 조건 우려로 중단
#### - `etl.daily_update`는 공식 API 적재기 전환이 끝날 때까지 실행을 차단


## 8/28 작업 기록(KRX 작업 멈춤, 공공데이터포털 OPEN API로 변경)

#### - `investor_flows`에 투자자별 순매수 수량(주) 열 추가
  - 외국인·기관·개인별 `*_net_volume` 3개 열을 추가하고 기존 DB에도 마이그레이션 적용
  - 수급 적재기가 순매수 거래대금(원)과 순매수 수량(주)을 함께 저장하도록 확장
#### - KRX 수급 적재 안정화(실패)
  - 종목 간 요청 대기 시간을 1초로 조정
  - 일시적인 KRX 오류에 대해 5초·10초·20초 점진 재시도 추가
  - 연속 5회 실패 시 대량 호출을 중단하도록 처리
  - 수량이 요청 종료일까지 적재된 종목은 백필 재실행 시 건너뛰도록 처리
#### - 수급 피처 모듈 추가
  - 외국인·기관·개인별 순매수 금액·수량의 1·5·20일 누적 피처 구현
  - 거래대금 대비 순매수 금액 비율 피처 구현
  - 수급 원천이 없는 경우 0으로 왜곡하지 않고 결측으로 유지하도록 테스트 완료
#### - 수집 현황 확인
  - 주가: 2,765종목, 2025-08-01~2026-08-28 적재 완료
  - 수급 금액: 328종목, 52,607건 적재 완료
  - 수급 수량: 0건 (KRX 웹 자동 수집은 재개하지 않음)
  - 업종·산업 프로필: 0건 (대체 공식 소스 선정 전 보류)

### 데이터 소스 전환 계획

#### 1. 일별 시세: 공공데이터포털 금융위원회 주식시세정보 API

#### - 무료 서비스 키 발급 후 OHLCV를 공식 REST API로 수집
#### - 기존 NAVER 웹 기반 시세 적재기를 API 기반으로 교체한 뒤 `daily_update`에 연결

#### 2. 재무·가치 지표: Open DART API

#### - 무료 인증키로 사업·분기보고서 재무제표를 수집
#### - 매출, 영업이익, 순이익, 자본을 적재하고 PER·PBR 등은 시점 일치 방식으로 계산

#### 3. 기술 지표: 로컬 계산

#### - API로 받은 OHLCV에서 RSI, MACD, 이동평균, 변동성, 거래량 지표를 계산

#### 4. 투자자 수급: 공식 사용 허가가 명확한 API를 확보할 때까지 모델 기본 입력에서 제외

#### - TradingView·Investing.com 웹 크롤링은 각 서비스 약관상 자동 수집이 금지되어 사용하지 않음

#### 5. 검증·모델 재학습

#### - 새 시세·재무 데이터 적재 후 피처 생성, 워크포워드 검증, 예측 모델 재학습

### 다음 작업

#### - 공공데이터포털 서비스 키와 Open DART 인증키를 발급받아 환경변수에 등록
#### - 공식 API 적재기 구현 및 기존 KRX 웹 의존 시세 경로 교체
#### - 기술·재무 피처를 학습 데이터셋에 연결하고 모델 재학습
